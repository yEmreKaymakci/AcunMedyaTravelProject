
@model List<AcunMedyaTravelProject.Entities.Booking2>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="container">
    <br /> <br />
    <h2>Reklam Listesi</h2>

    <table class="table table-bordered">
        <thead>
            <tr>
                <td>ID</td>
                <td>Başlık</td>
                <td>Seyahat Süresi</td>
                <td>Ücreti</td>
                <td>Resim</td>
                <td>Güncelle</td>
                <td>Sil</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Booking2ID</td>
                    <td>@item.Title</td>
                    <td>@item.Date</td>
                    <td>@item.Description</td>
                    <td><img src="@item.ImageUrl" style="width:50px;height:50px" /></td>
                    <td><a href="/Advert/UpdateAdvert/@item.Booking2ID" class="btn btn-success">Güncelle</a></td>
                    <td><button class="btn btn-danger" onclick="Delete(@item.Booking2ID)">Sil</button></td>
                </tr>
            }
        </tbody>
    </table>
    <br /> <br />
    <a href="/Advert/AddAdvert/" class="btn btn-dark">Ekle</a>
</div>

<script>
    function Delete(id) {
        Swal.fire({
            title: "Emin misin?",
            text: "Dikkat bu işlemi geri alamazsınız!",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#3085d6",
            cancelButtonColor: "#d33",
            confirmButtonText: "Evet, silin!",
            cancelButtonText: "İptal"
        }).then((result) => {
            if (result.isConfirmed) {
                Swal.fire({
                    title: "Silindi!",
                    text: "Veri silindi.",
                    icon: "success",
                    allowOutsideClick: false
                }).then((result) => {
                    window.location.href = '/Advert/DeleteAdvert/' + id;
                });
            }
        });
    }</script>
